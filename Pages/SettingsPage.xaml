<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="WsaPacman.Pages.SettingsPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    FontFamily="Yu Gothic UI, Segoe UI">

    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <StackPanel Margin="24,0,24,32" Spacing="4">

            <!-- ページタイトル -->
            <TextBlock
                Text="設定"
                Style="{StaticResource TitleTextBlockStyle}"
                Margin="0,16,0,16"/>

            <!-- ═══════════════════════════════════════════════════════════════
                 接続設定
            ═══════════════════════════════════════════════════════════════ -->
            <TextBlock
                Text="接続"
                Style="{StaticResource BodyStrongTextBlockStyle}"
                Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                Margin="4,4,0,4"/>

            <!-- ADB ポート -->
            <Border Style="{StaticResource SettingCardStyle}">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <FontIcon Grid.Column="0" Glyph="&#xE701;" FontSize="{StaticResource SettingIconSize}" VerticalAlignment="Center" Margin="0,0,14,0"/>
                    <TextBlock Grid.Column="1" Text="ADB ポート" VerticalAlignment="Center" FontSize="14"/>
                    <StackPanel Grid.Column="2" Orientation="Horizontal" Spacing="6" VerticalAlignment="Center">
                        <TextBlock Text="127.0.0.1 :" VerticalAlignment="Center" Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
                        <NumberBox
                            x:Name="AndroidPortBox"
                            Value="58526"
                            Minimum="1"
                            Maximum="65535"
                            SpinButtonPlacementMode="Compact"
                            Width="120"
                            ValueChanged="AndroidPortBox_ValueChanged"/>
                        <Button
                            ToolTipService.ToolTip="デフォルトに戻す (58526)"
                            Padding="6"
                            Click="BtnResetPort_Click">
                            <SymbolIcon Symbol="Refresh"/>
                        </Button>
                    </StackPanel>
                </Grid>
            </Border>

            <!-- ═══════════════════════════════════════════════════════════════
                 動作設定
            ═══════════════════════════════════════════════════════════════ -->
            <TextBlock
                Text="動作"
                Style="{StaticResource BodyStrongTextBlockStyle}"
                Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                Margin="4,12,0,4"/>

            <!-- WSA 自動起動 -->
            <Border Style="{StaticResource SettingCardStyle}">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <FontIcon Grid.Column="0" Glyph="&#xE7E8;" FontSize="{StaticResource SettingIconSize}" VerticalAlignment="Center" Margin="0,0,14,0"/>
                    <StackPanel Grid.Column="1" VerticalAlignment="Center">
                        <TextBlock Text="WSA を自動起動" FontSize="14"/>
                        <TextBlock Text="APK インストール時に WSA が停止中であれば自動で起動します" Style="{StaticResource CaptionTextBlockStyle}" Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
                    </StackPanel>
                    <ToggleSwitch
                        Grid.Column="2"
                        x:Name="AutostartToggle"
                        Margin="0"
                        OnContent=""
                        OffContent=""
                        Toggled="AutostartToggle_Toggled"/>
                </Grid>
            </Border>

            <!-- レジストリ自動バックアップ -->
            <Border Style="{StaticResource SettingCardStyle}">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <SymbolIcon Grid.Column="0" Symbol="Save" VerticalAlignment="Center" Margin="0,0,14,0"/>
                    <StackPanel Grid.Column="1" VerticalAlignment="Center">
                        <TextBlock Text="アンインストール前にレジストリを自動バックアップ" FontSize="14"/>
                        <TextBlock Text="クリーンアップ実行前にレジストリを自動保存します" Style="{StaticResource CaptionTextBlockStyle}" Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
                    </StackPanel>
                    <ToggleSwitch
                        Grid.Column="2"
                        x:Name="AutoBackupToggle"
                        Margin="0"
                        OnContent=""
                        OffContent=""
                        Toggled="AutoBackupToggle_Toggled"/>
                </Grid>
            </Border>

            <!-- インストールタイムアウト -->
            <Border Style="{StaticResource SettingCardStyle}">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="300"/>
                    </Grid.ColumnDefinitions>
                    <FontIcon Grid.Column="0" Glyph="&#xE916;" FontSize="{StaticResource SettingIconSize}" VerticalAlignment="Center" Margin="0,0,14,0"/>
                    <StackPanel Grid.Column="1" VerticalAlignment="Center">
                        <TextBlock x:Name="TimeoutLabel" Text="インストールタイムアウト: 60 秒" FontSize="14"/>
                        <TextBlock Text="0 で無制限" Style="{StaticResource CaptionTextBlockStyle}" Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
                    </StackPanel>
                    <Slider
                        Grid.Column="2"
                        x:Name="TimeoutSlider"
                        Minimum="0"
                        Maximum="120"
                        Value="60"
                        TickFrequency="15"
                        TickPlacement="Outside"
                        VerticalAlignment="Center"
                        ValueChanged="TimeoutSlider_ValueChanged"/>
                </Grid>
            </Border>

            <!-- バックアップ保存先 -->
            <Border Style="{StaticResource SettingCardStyle}">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <SymbolIcon Grid.Column="0" Symbol="Folder" VerticalAlignment="Center" Margin="0,0,14,0"/>
                    <StackPanel Grid.Column="1" VerticalAlignment="Center">
                        <TextBlock Text="バックアップ保存先" FontSize="14"/>
                        <TextBlock
                            x:Name="BackupDirText"
                            Text="%USERPROFILE%\Desktop"
                            Style="{StaticResource CaptionTextBlockStyle}"
                            Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
                    </StackPanel>
                    <StackPanel Grid.Column="2" Orientation="Horizontal" Spacing="8" VerticalAlignment="Center">
                        <Button
                            x:Name="BtnResetBackupDir"
                            ToolTipService.ToolTip="デスクトップにリセット"
                            Padding="6"
                            Visibility="Collapsed"
                            Click="BtnResetBackupDir_Click">
                            <SymbolIcon Symbol="Clear"/>
                        </Button>
                        <Button
                            x:Name="BtnBrowseBackupDir"
                            Padding="12,6"
                            Click="BtnBrowseBackupDir_Click">
                            <TextBlock Text="参照" FontWeight="SemiBold"/>
                        </Button>
                    </StackPanel>
                </Grid>
            </Border>

            <!-- ═══════════════════════════════════════════════════════════════
                 表示設定
            ═══════════════════════════════════════════════════════════════ -->
            <TextBlock
                Text="表示"
                Style="{StaticResource BodyStrongTextBlockStyle}"
                Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                Margin="4,12,0,4"/>

            <!-- 言語 -->
            <Border Style="{StaticResource SettingCardStyle}">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="200"/>
                    </Grid.ColumnDefinitions>
                    <FontIcon Grid.Column="0" Glyph="&#xF2B7;" FontSize="{StaticResource SettingIconSize}" VerticalAlignment="Center" Margin="0,0,14,0"/>
                    <TextBlock Grid.Column="1" Text="言語" VerticalAlignment="Center" FontSize="14"/>
                    <ComboBox
                        Grid.Column="2"
                        x:Name="LanguageComboBox"
                        HorizontalAlignment="Stretch"
                        SelectionChanged="LanguageComboBox_SelectionChanged">
                        <ComboBoxItem Content="システム" IsSelected="True"/>
                        <ComboBoxItem Content="日本語 (ja)"/>
                        <ComboBoxItem Content="English (en)"/>
                    </ComboBox>
                </Grid>
            </Border>

            <!-- テーマ -->
            <Expander
                x:Name="ThemeExpander"
                HorizontalAlignment="Stretch"
                HorizontalContentAlignment="Stretch"
                IsExpanded="False"
                Margin="0,0,0,4">
                <Expander.Header>
                    <Grid Margin="0,4">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <FontIcon Grid.Column="0" Glyph="&#xE793;" FontSize="{StaticResource SettingIconSize}" VerticalAlignment="Center" Margin="0,0,14,0"/>
                        <TextBlock Grid.Column="1" Text="テーマモード" VerticalAlignment="Center" FontSize="14"/>
                    </Grid>
                </Expander.Header>
                <StackPanel Spacing="10" Margin="36,8,0,4">
                    <RadioButton x:Name="ThemeSystem"  Content="システム設定に従う" GroupName="ThemeGroup" IsChecked="True" Checked="ThemeRadio_Checked"/>
                    <RadioButton x:Name="ThemeLight"   Content="ライト"             GroupName="ThemeGroup" Checked="ThemeRadio_Checked"/>
                    <RadioButton x:Name="ThemeDark"    Content="ダーク"             GroupName="ThemeGroup" Checked="ThemeRadio_Checked"/>
                </StackPanel>
            </Expander>

            <!-- アクセントカラー -->
            <Expander
                x:Name="AccentExpander"
                HorizontalAlignment="Stretch"
                HorizontalContentAlignment="Stretch"
                IsExpanded="False"
                Margin="0,0,0,4">
                <Expander.Header>
                    <Grid Margin="0,4">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <FontIcon Grid.Column="0" Glyph="&#xE790;" FontSize="{StaticResource SettingIconSize}" VerticalAlignment="Center" Margin="0,0,14,0"/>
                        <TextBlock Grid.Column="1" Text="アクセントカラー" VerticalAlignment="Center" FontSize="14"/>
                    </Grid>
                </Expander.Header>
                <StackPanel Spacing="16" Margin="36,8,0,8">
                    <!-- カラーパレット (丸いボタン) -->
                    <StackPanel Orientation="Horizontal" Spacing="8">
                        <!-- システム (OS アクセントカラー) -->
                        <Button x:Name="ColorSystem"  Width="32" Height="32" CornerRadius="16" Padding="0" ToolTipService.ToolTip="システム"  Click="ColorBtn_Click" Tag="System"  Background="#0078D4"/>
                        <Button x:Name="ColorDefault" Width="32" Height="32" CornerRadius="16" Padding="0" ToolTipService.ToolTip="デフォルト" Click="ColorBtn_Click" Tag="Default" Background="#2D7D6F"/>
                        <Button x:Name="ColorYellow"  Width="32" Height="32" CornerRadius="16" Padding="0" ToolTipService.ToolTip="イエロー"  Click="ColorBtn_Click" Tag="Yellow"  Background="#FFB900"/>
                        <Button x:Name="ColorOrange"  Width="32" Height="32" CornerRadius="16" Padding="0" ToolTipService.ToolTip="オレンジ"  Click="ColorBtn_Click" Tag="Orange"  Background="#F7630C"/>
                        <Button x:Name="ColorRed"     Width="32" Height="32" CornerRadius="16" Padding="0" ToolTipService.ToolTip="レッド"    Click="ColorBtn_Click" Tag="Red"     Background="#E74856"/>
                        <Button x:Name="ColorMagenta" Width="32" Height="32" CornerRadius="16" Padding="0" ToolTipService.ToolTip="マゼンタ"  Click="ColorBtn_Click" Tag="Magenta" Background="#BF0077"/>
                        <Button x:Name="ColorPurple"  Width="32" Height="32" CornerRadius="16" Padding="0" ToolTipService.ToolTip="パープル"  Click="ColorBtn_Click" Tag="Purple"  Background="#744DA9"/>
                        <Button x:Name="ColorBlue"    Width="32" Height="32" CornerRadius="16" Padding="0" ToolTipService.ToolTip="ブルー"    Click="ColorBtn_Click" Tag="Blue"    Background="#0078D4"/>
                        <Button x:Name="ColorTeal"    Width="32" Height="32" CornerRadius="16" Padding="0" ToolTipService.ToolTip="ティール"  Click="ColorBtn_Click" Tag="Teal"    Background="#038387"/>
                        <Button x:Name="ColorGreen"   Width="32" Height="32" CornerRadius="16" Padding="0" ToolTipService.ToolTip="グリーン"  Click="ColorBtn_Click" Tag="Green"   Background="#107C10"/>
                    </StackPanel>
                    <!-- カスタムカラー (将来的にカラーピッカーを表示) -->
                    <Button x:Name="BtnCustomColor" Click="BtnCustomColor_Click" Padding="12,6">
                        <StackPanel Orientation="Horizontal" Spacing="8">
                            <Ellipse x:Name="CustomColorPreview" Width="16" Height="16" Fill="#0078D4"/>
                            <TextBlock Text="カスタムカラー…"/>
                        </StackPanel>
                    </Button>
                </StackPanel>
            </Expander>

            <!-- Mica (Windows 11 のみ表示する想定) -->
            <Expander
                x:Name="MicaExpander"
                HorizontalAlignment="Stretch"
                HorizontalContentAlignment="Stretch"
                IsExpanded="False"
                Margin="0,0,0,4">
                <Expander.Header>
                    <Grid Margin="0,4">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <FontIcon Grid.Column="0" Glyph="&#xE81A;" FontSize="{StaticResource SettingIconSize}" VerticalAlignment="Center" Margin="0,0,14,0"/>
                        <TextBlock Grid.Column="1" Text="背景エフェクト (Mica)" VerticalAlignment="Center" FontSize="14"/>
                    </Grid>
                </Expander.Header>
                <StackPanel Spacing="10" Margin="36,8,0,4">
                    <RadioButton x:Name="MicaDisabled" Content="無効 (ソリッドカラー)"     GroupName="MicaGroup" Checked="MicaRadio_Checked"/>
                    <RadioButton x:Name="MicaEnabled"  Content="Mica (Base) — 標準"        GroupName="MicaGroup" IsChecked="True" Checked="MicaRadio_Checked"/>
                    <RadioButton x:Name="MicaAlt"      Content="Mica Alt — より濃い Mica"  GroupName="MicaGroup" Checked="MicaRadio_Checked"/>
                </StackPanel>
            </Expander>

            <!-- アイコンスタイル -->
            <Expander
                x:Name="IconExpander"
                HorizontalAlignment="Stretch"
                HorizontalContentAlignment="Stretch"
                IsExpanded="False"
                Margin="0,0,0,4">
                <Expander.Header>
                    <Grid Margin="0,4">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <FontIcon Grid.Column="0" Glyph="&#xE91B;" FontSize="{StaticResource SettingIconSize}" VerticalAlignment="Center" Margin="0,0,14,0"/>
                        <TextBlock Grid.Column="1" Text="アダプティブアイコン" VerticalAlignment="Center" FontSize="14"/>
                        <!-- ヘッダー内トグルスイッチ (レガシーアイコンのオン/オフ) -->
                        <StackPanel Grid.Column="2" Orientation="Horizontal" Spacing="8" Margin="0,0,12,0">
                            <TextBlock x:Name="LegacyIconLabel" Text="オン" VerticalAlignment="Center" Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
                            <ToggleSwitch
                                x:Name="LegacyIconToggle"
                                IsOn="True"
                                Margin="0"
                                OnContent=""
                                OffContent=""
                                Toggled="LegacyIconToggle_Toggled"/>
                        </StackPanel>
                    </Grid>
                </Expander.Header>
                <StackPanel Spacing="10" Margin="36,8,0,4">
                    <RadioButton x:Name="IconCircle"    Content="円形"         GroupName="IconGroup" IsChecked="True" Checked="IconRadio_Checked"/>
                    <RadioButton x:Name="IconRounded"   Content="角丸正方形"   GroupName="IconGroup" Checked="IconRadio_Checked"/>
                    <RadioButton x:Name="IconSquircle"  Content="スクワークル" GroupName="IconGroup" Checked="IconRadio_Checked"/>
                </StackPanel>
            </Expander>

            <!-- ── アプリ情報 ──────────────────────────────────────────────────── -->
            <TextBlock
                Text="情報"
                Style="{StaticResource BodyStrongTextBlockStyle}"
                Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                Margin="4,12,0,4"/>
            <Border Style="{StaticResource SettingCardStyle}">
                <StackPanel Spacing="2">
                    <TextBlock Text="WSA Package Manager" Style="{StaticResource BodyStrongTextBlockStyle}"/>
                    <TextBlock Text="バージョン 1.6.0" Style="{StaticResource CaptionTextBlockStyle}" Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
                </StackPanel>
            </Border>

        </StackPanel>
    </ScrollViewer>
</Page>
