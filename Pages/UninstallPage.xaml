<?xml version="1.0" encoding="utf-8" ?>
<Page
    x:Class="WsaPacman.Pages.UninstallPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="using:WsaPacman.Pages"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <!-- Command bar -->
        <Border
            Grid.Row="0"
            Padding="24,12,24,12"
            BorderBrush="{ThemeResource DividerStrokeColorDefaultBrush}"
            BorderThickness="0,0,0,1">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>

                <StackPanel Grid.Column="0" Orientation="Horizontal" Spacing="8">
                    <Button
                        x:Name="BtnScan"
                        AutomationProperties.AutomationId="BtnScan"
                        AutomationProperties.Name="{x:Bind R.scan_system}"
                        Click="Scan_Click"
                        Style="{StaticResource AccentButtonStyle}">
                        <StackPanel Orientation="Horizontal" Spacing="8">
                            <FontIcon FontSize="14" Glyph="&#xE721;" />
                            <TextBlock x:Name="ScanButtonText" Text="{x:Bind R.scan_system}" />
                        </StackPanel>
                    </Button>
                    <Button
                        AutomationProperties.AutomationId="BtnBackupRegistry"
                        AutomationProperties.Name="{x:Bind R.backup_registry}"
                        Click="BackupRegistry_Click">
                        <StackPanel Orientation="Horizontal" Spacing="8">
                            <FontIcon FontSize="14" Glyph="&#xE74E;" />
                            <TextBlock Text="{x:Bind R.backup_registry}" />
                        </StackPanel>
                    </Button>
                </StackPanel>

                <Button
                    x:Name="BtnCleanup"
                    Grid.Column="1"
                    AutomationProperties.AutomationId="BtnCleanup"
                    AutomationProperties.Name="{x:Bind R.execute_cleanup}"
                    Click="Cleanup_Click"
                    IsEnabled="False"
                    Foreground="{ThemeResource SystemErrorTextColor}">
                    <StackPanel Orientation="Horizontal" Spacing="8">
                        <FontIcon FontSize="14" Glyph="&#xE74D;" />
                        <TextBlock Text="{x:Bind R.execute_cleanup}" />
                    </StackPanel>
                </Button>
            </Grid>
        </Border>

        <!-- Content area -->
        <Grid Grid.Row="1" Padding="24,0,24,0">

            <!-- Empty / scanning state -->
            <StackPanel
                x:Name="EmptyState"
                HorizontalAlignment="Center"
                VerticalAlignment="Center"
                Spacing="16">
                <TextBlock
                    x:Name="EmptyStateText"
                    Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                    HorizontalAlignment="Center"
                    Text="{x:Bind R.click_scan_to_find_ghost_apps}" />
            </StackPanel>

            <!-- Scanning indicator -->
            <StackPanel
                x:Name="ScanningState"
                HorizontalAlignment="Center"
                VerticalAlignment="Center"
                Spacing="16"
                Visibility="Collapsed">
                <ProgressRing
                    Width="44"
                    Height="44"
                    IsActive="True" />
                <TextBlock
                    Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                    HorizontalAlignment="Center"
                    Text="{x:Bind R.scanning}" />
            </StackPanel>

            <!-- Results list -->
            <ListView
                x:Name="AppList"
                Padding="0,12,0,12"
                SelectionMode="None"
                Visibility="Collapsed">
                <ListView.ItemTemplate>
                    <DataTemplate x:DataType="local:AppEntry">
                        <Grid Padding="4,8,4,8">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto" />
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="Auto" />
                            </Grid.ColumnDefinitions>
                            <FontIcon
                                Grid.Column="0"
                                Margin="0,0,12,0"
                                FontSize="28"
                                Glyph="&#xECAA;"
                                VerticalAlignment="Center" />
                            <StackPanel Grid.Column="1" VerticalAlignment="Center">
                                <TextBlock
                                    Style="{StaticResource BodyStrongTextBlockStyle}"
                                    Text="{x:Bind Name}" />
                                <TextBlock
                                    Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                                    Style="{StaticResource CaptionTextBlockStyle}"
                                    Text="{x:Bind PackageId}" />
                            </StackPanel>
                            <CheckBox
                                Grid.Column="2"
                                AutomationProperties.Name="{x:Bind Name}"
                                IsChecked="{x:Bind IsSelected, Mode=TwoWay}"
                                VerticalAlignment="Center" />
                        </Grid>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
        </Grid>
    </Grid>
</Page>
